# Dependencies (reinstall with `npm install`) — never commit these
node_modules/

# Logs
*.log
npm-debug.log*

# Environment / secrets
.env
# MySQL password lives here — each dev creates their own copy locally
server/db.config.js

# OS / editor cruft
Thumbs.db
.DS_Store
